# 市场简报自动更新 — 设置说明

这套流程会：每天盘中自动刷新数据 → 发布成公开网页 → 同时发邮件通知你。
全部用免费服务搭建（GitHub + Finnhub + Gmail）。

---

## 第一步：注册免费数据源（Finnhub）

1. 打开 https://finnhub.io/register
2. 注册一个免费账号
3. 登录后在 Dashboard 首页能看到你的 **API Key**，复制下来备用

> 免费版有请求次数限制（每分钟60次），本项目每次运行只请求7个代码，完全够用。

---

## 第二步：创建 GitHub 仓库

1. 打开 https://github.com/new
2. 仓库名随意，比如 `market-briefing`
3. 设为 **Public**（GitHub Pages 免费版需要公开仓库）
4. 创建完成后，把我给你的这几个文件全部上传上去，保持文件夹结构：

```
market-briefing/
├── .github/
│   └── workflows/
│       └── update.yml
├── docs/                  ← 会自动生成，先建个空文件夹即可
├── fetch_and_build.py
├── template.html
├── requirements.txt
└── 设置说明.md
```

**上传方法**：直接在 GitHub 网页点 "Add file" → "Upload files"，把文件拖进去即可，不需要用命令行。

---

## 第三步：设置密钥（Secrets）

在你的仓库页面：

1. 点 **Settings** → 左侧菜单 **Secrets and variables** → **Actions**
2. 点 **New repository secret**，依次添加以下几个：

| Secret 名称 | 值 |
|---|---|
| `FINNHUB_API_KEY` | 第一步拿到的 Finnhub API Key |
| `EMAIL_USERNAME` | 你的 Gmail 邮箱地址 |
| `EMAIL_PASSWORD` | Gmail **应用专用密码**（见下方说明，不是你的登录密码） |
| `EMAIL_TO` | 你想接收通知的邮箱地址 |

### 如何获取 Gmail 应用专用密码

1. 打开 https://myaccount.google.com/apppasswords
2. 需要先开启"两步验证"才能看到这个选项
3. 生成一个应用密码（选择"邮件"类型），会得到一个16位密码
4. 把这个密码填入 `EMAIL_PASSWORD`（不是你平时登录用的密码）

---

## 第四步：开启 GitHub Pages

1. 仓库页面 → **Settings** → 左侧菜单 **Pages**
2. **Source** 选择 `Deploy from a branch`
3. Branch 选择 `main`，文件夹选择 `/docs`
4. 保存后，几分钟内你会看到一个公开网址，形如：
   ```
   https://你的用户名.github.io/market-briefing/
   ```
   这就是你以后随时能打开的公开网页链接。

---

## 第五步：测试运行

1. 仓库页面 → **Actions** 标签
2. 左侧选 "Update Market Briefing"
3. 点右侧 **Run workflow** 按钮，手动触发一次
4. 等待约1分钟，检查：
   - 是否收到邮件
   - 打开你的 GitHub Pages 网址，看数据是否更新

---

## 之后就完全自动了

工作日盘中会自动运行 5 次（开盘、盘中3次、收盘附近），每次都会：
- 刷新网页上的实时数据
- 发一封邮件给你

你不需要再做任何操作，也不需要打开 Claude —— 网页链接可以直接存到手机浏览器书签或主屏幕。

---

## 想要调整的地方

- **修改运行时间**：编辑 `.github/workflows/update.yml` 里的 `cron` 那几行
- **增加更多股票/ETF**：编辑 `fetch_and_build.py` 里的 `TICKERS` 字典
- **换成其他邮箱服务商**（非 Gmail）：修改 workflow 里的 `server_address` 和 `server_port`

有任何一步卡住了，把报错信息发给我，我可以帮你看。

# 市场简报 v2 — 升级版设置说明（Yahoo Finance 版）

这版加了：**真实指数**（标普500/纳斯达克/道指/VIX，不是ETF代理）、
历史图表（1M/1Y/5Y）、固定收益（国债收益率/通胀）、
热门话题（真实新闻标题+链接）、近期IPO。

指数和图表数据用 **Yahoo Finance 免费接口**（不需要注册、不需要API Key）。
只有固定收益和IPO两块需要注册免费账号。

⚠️ **已知风险**：Yahoo 这个接口是非官方的，没有公开文档，理论上随时
可能变化或被限速。如果某天突然抓取失败（数字全变成0），大概率是
Yahoo接口临时问题，等一会儿重跑通常会恢复；如果长期失效，需要换回
付费/官方数据源（Finnhub/Twelve Data），到时候可以再找我调整。

---

## 架构说明

拆成两个自动任务：

| 任务 | 频率 | 做什么 |
|---|---|---|
| **Daily Full Refresh** | 每个工作日1次（开盘前） | 抓历史图表（Yahoo）、固定收益（FRED）、新闻标题（RSS）、IPO（FMP），存到 `data/daily.json` |
| **Intraday Price Refresh** | 每个工作日4次（盘中+收盘） | 只刷新指数/期货实时报价（Yahoo），其余内容用当天已抓好的缓存数据 |

---

## 第一步：注册2个免费数据源（比上一版少了一个）

### 1. FRED（美联储数据 — 国债收益率/通胀）
1. https://fred.stlouisfed.org/docs/api/api_key.html
2. 点 "Request API Key"，用邮箱注册（完全免费，无需信用卡）
3. 拿到一个 32位字符的 API Key

### 2. Financial Modeling Prep（IPO日历）
1. https://site.financialmodelingprep.com/developer/docs
2. 注册免费账号
3. Dashboard 里能看到 API Key

**不再需要 Finnhub 和 Twelve Data 账号**——指数、黄金、原油、VIX全部
换成 Yahoo Finance 免费接口，不用注册。

---

## 第二步：把新文件上传到你已有的 GitHub 仓库

这次给你的文件里，有些是**替换**旧文件，有些是**新增**：

```
Market_News/
├── .github/
│   └── workflows/
│       ├── update-daily.yml      ← 新增（替代旧的 update.yml）
│       └── update-prices.yml     ← 新增
├── data/
│   └── daily.json                ← 新增（占位文件，先建空的）
├── docs/                         ← 已存在，不用管
├── fetch_prices.py               ← 新增（替代旧的 fetch_and_build.py）
├── generate_daily.py             ← 新增
├── template.html                 ← 替换旧的
└── requirements.txt              ← 替换旧的
```

**上传步骤：**
1. 先删除旧的 `.github/workflows/update.yml` 和 `fetch_and_build.py`（在文件页面点右上角垂圾桶图标）
2. 用 "Add file → Upload files" 把新文件传上去，注意 `data/daily.json` 需要用
   "Create new file" 方式建，文件名输入 `data/daily.json`，内容粘贴我给的占位JSON

---

## 第三步：新增2个 Secrets（并删除旧的2个）

在 **Settings → Secrets and variables → Actions**：

**新增：**

| Secret 名称 | 值 |
|---|---|
| `FRED_API_KEY` | 第一步拿到的 FRED key |
| `FMP_API_KEY` | 第一步拿到的 FMP key |

**删除**（不再需要，点旁边的垃圾桶图标删掉）：
- `FINNHUB_API_KEY`
- `TWELVEDATA_API_KEY`（如果之前加过）

**保留不变：**
- `EMAIL_USERNAME`、`EMAIL_PASSWORD`、`EMAIL_TO`

---

## 第四步：测试运行

1. **先手动跑一次 "Daily Full Refresh"**（Actions 标签 → 选它 → Run workflow）
   这一步会生成 `data/daily.json`，没有这一步图表会是空的
2. 等成功后，再手动跑一次 **"Intraday Price Refresh"**
3. 打开你的网页链接，确认：
   - 图表有没有显示出曲线
   - 固定收益数字有没有出现
   - 热门话题下面是不是真实新闻标题（点击能跳转）
   - IPO表格有没有数据（如果最近60天没有新IPO，这里可能是空的，正常）

---

## 常见问题

**Q: 热门话题是空的？**
A: 某些RSS源偶尔会抓取失败（网络波动），下次运行通常会恢复。也可以在 Actions 运行记录里看报错。

**Q: IPO表格是空的？**
A: 正常，取决于最近60天是否有新股上市，FMP免费版数据有时会有延迟。

**Q: 图表没有曲线，或者指数显示 0.00？**
A: 先确认 "Daily Full Refresh" 跑成功过一次——图表数据依赖它生成的
`data/daily.json`。如果那次也失败了，大概率是 Yahoo Finance 接口临时
限速或波动，等几分钟手动重跑一次通常能恢复。

**Q: 想问某个新闻/术语的深度解读？**
A: 直接回到这个 Claude 对话里问我，网页本身不做AI问答（为了不产生额外API费用）。
